# CLAUDE.md

Read **[AGENTS.md](AGENTS.md)** — it holds this project's working notes, commands,
architecture, invariants, and conventions.

That file is deliberately tool-agnostic (the `AGENTS.md` convention is read by Codex,
Cursor, Aider, and others), so the guidance stays in one place no matter which assistant
is driving. This file exists only so Claude Code finds its way there.

Put new project guidance in `AGENTS.md`, not here.
